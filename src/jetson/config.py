"""
Jetbotmini 水果追踪 - Jetson Nano 端配置文件
作用：统一管理网络参数、摄像头参数
"""
import os

# ===================== 网络配置 =====================
# 支持环境变量，方便不同网络环境切换，不用每次改代码
CAR_IP = os.getenv("CAR_IP", "192.168.1.4")     # Jetson Nano IP
PC_IP = os.getenv("PC_IP", "192.168.1.10")       # 电脑IP
VIDEO_PORT = 8080          # 画面传输端口
CMD_PORT = 8081            # 控制指令端口

# ===================== 摄像头配置 =====================
CAMERA_WIDTH = 1280          # 摄像头分辨率宽度（改成1280可提高清晰度，但会增加延迟）
CAMERA_HEIGHT = 720        # 摄像头分辨率高度
JPEG_QUALITY = 95          # JPEG压缩质量

# ===================== 小车运动参数 =====================
FORWARD_SPEED = 1.0        # 前进速度（0-1）
TURN_SPEED = 0.8           # 转弯速度（0-1）
BACKWARD_SPEED = 1.0       # 后退速度（0-1）

# ===================== 使用说明 =====================
# 如果IP经常变化，可以在启动前设置环境变量：
# export CAR_IP=192.168.1.20
# export PC_IP=192.168.1.30
# python nano_main.py
